- name: cloudtrail_defaults
  description: CloudTrail default configurations
  configuration_parameters:
    BUCKET_TYPE: cloudtrail
    BUCKET_NAME: guardsarm-cloudtrail-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: cloudtrail
    bucket_name: guardsarm-cloudtrail-integration-tests

- name: vpc_defaults
  description: VPC default configurations
  configuration_parameters:
    BUCKET_TYPE: vpcflow
    BUCKET_NAME: guardsarm-vpcflow-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: vpcflow
    bucket_name: guardsarm-vpcflow-integration-tests

- name: config_defaults
  description: Config default configurations
  configuration_parameters:
    BUCKET_TYPE: config
    BUCKET_NAME: guardsarm-config-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: config
    bucket_name: guardsarm-config-integration-tests

- name: alb_defaults
  description: ALB default configurations
  configuration_parameters:
    BUCKET_TYPE: alb
    BUCKET_NAME: guardsarm-alb-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: alb
    bucket_name: guardsarm-alb-integration-tests

- name: clb_defaults
  description: CLB default configurations
  configuration_parameters:
    BUCKET_TYPE: clb
    BUCKET_NAME: guardsarm-clb-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: clb
    bucket_name: guardsarm-clb-integration-tests

- name: nlb_defaults
  description: NLB default configurations
  configuration_parameters:
    BUCKET_TYPE: nlb
    BUCKET_NAME: guardsarm-nlb-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: nlb
    bucket_name: guardsarm-nlb-integration-tests

- name: kms_defaults
  description: KMS default configurations
  configuration_parameters:
    BUCKET_TYPE: custom
    BUCKET_NAME: guardsarm-kms-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: custom
    bucket_name: guardsarm-kms-integration-tests

- name: macie_defaults
  description: CloudTrail default configurations
  configuration_parameters:
    BUCKET_TYPE: custom
    BUCKET_NAME: guardsarm-macie-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: custom
    bucket_name: guardsarm-macie-integration-tests

- name: trusted_advisor_defaults
  description: Trusted Advisor default configurations
  configuration_parameters:
    BUCKET_TYPE: custom
    BUCKET_NAME: guardsarm-trusted-advisor-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: custom
    bucket_name: guardsarm-trusted-advisor-integration-tests

- name: guardduty_defaults
  description: GuardDuty default configurations
  configuration_parameters:
    BUCKET_TYPE: guardduty
    BUCKET_NAME: guardsarm-guardduty-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: guardduty
    bucket_name: guardsarm-guardduty-integration-tests

- name: native_guardduty_defaults
  description: Native GuardDuty default configurations
  configuration_parameters:
    BUCKET_TYPE: guardduty
    BUCKET_NAME: guardsarm-native-guardduty-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: guardduty
    bucket_name: guardsarm-native-guardduty-integration-tests

- name: waf_defaults
  description: WAF default configurations
  configuration_parameters:
    BUCKET_TYPE: waf
    BUCKET_NAME: guardsarm-waf-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: waf
    bucket_name: guardsarm-waf-integration-tests

- name: server_access_defaults
  description: Server Access default configurations
  configuration_parameters:
    BUCKET_TYPE: server_access
    BUCKET_NAME: guardsarm-server-access-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: server_access
    bucket_name: guardsarm-server-access-integration-tests

- name: cisco_umbrella_defaults
  description: Umbrella default configurations
  configuration_parameters:
    BUCKET_TYPE: cisco_umbrella
    BUCKET_NAME: guardsarm-umbrella-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: cisco_umbrella
    bucket_name: guardsarm-umbrella-integration-tests
