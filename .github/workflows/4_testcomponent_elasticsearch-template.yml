name: 4.X - guardsarm-template for Elasticsearch test

# if the file guardsarm/extensions/elasticsearch/7.x/guardsarm-template.json is modified launch the test or is a part of
# a pull request

on:
  push:
    branches:
      - "[0-4].[0-9]+.[0-9]+"
    paths:
      - "extensions/elasticsearch/7.x/**"

jobs:
  deprecated:
    runs-on: ubuntu-24.04
    steps:
      - name: Warning
        run: echo "This workflow is deprecated. Be careful, you have just run it from main."
