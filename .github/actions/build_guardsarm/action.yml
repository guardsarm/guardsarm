name: "Build GuardSarm"
description: "Builds the GuardSarm project using make for a given target"

inputs:
  target:
    required: false
    description: "Build target: agent or manager"
    default: "agent"

runs:
  using: "composite"
  steps:
    - name: Build GuardSarm ${{ inputs.target }}
      shell: bash
      env:
        TARGET: ${{ inputs.target }}
      run: |
        make deps -C src TARGET="$TARGET" -j$(nproc)
        make -C src TARGET="$TARGET" -j$(nproc)
